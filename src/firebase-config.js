// Firebase web config. This is NOT a secret — Firebase web configs are public
// by design; access is controlled by database rules, not by hiding this file.
//
// Fill this in from: Firebase console > Project settings > Your apps > Web app.
// Leave it as null to skip probe 4; probes 1-3 run without a Firebase project
// and already answer most of the connectivity question.
//
// Only databaseURL and apiKey/projectId matter for a Realtime Database probe.
export const firebaseConfig = null;

// Example once you have a project:
//
// export const firebaseConfig = {
//   apiKey: "AIza...",
//   authDomain: "hangman-xxxx.firebaseapp.com",
//   databaseURL: "https://hangman-xxxx-default-rtdb.firebaseio.com",
//   projectId: "hangman-xxxx",
// };
